<!-- GENERATED by tools/package_docs.py. Edit this tool metadata or replace the file and remove this marker. -->

# symstrategies

`CAIMEOX/symbit/symstrategies` is the public import path for this package.

Reusable strategy combinators for deterministic expression rewriting, tree rewrites, branch exploration, and traversal orchestration.

## When To Use This Package

- Import `CAIMEOX/symbit/symstrategies` directly when your code depends on this package's subsystem-specific types or algorithms.
- Prefer this package over the root facade when you want the focused API surface listed below rather than a convenience wrapper.
- Treat the example in this README as a regression test: if the public calling style changes, `moon test` should catch the drift.

## Key Public Entry Points

- `basic_fns`
- `canon`
- `chain`
- `condition`
- `debug`
- `distribute`
- `do_one`
- `exhaust`

## Example

```mbt check
///|
test "symstrategies public API is available" {
  let _ = basic_fns
  inspect(true, content="true")
}
```

## Related Packages

- `CAIMEOX/symbit/symcore`
- `CAIMEOX/symbit/symsimplify`

## Further Reading

- [`../../docs/specs/strategies.md`](../../docs/specs/strategies.md)
